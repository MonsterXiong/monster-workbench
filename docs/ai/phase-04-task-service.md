# Phase 04：TaskService 与 Commands

## 对 Codex 说

```text
请执行 docs/goals/goal_02_task_service_commands.md。

要求：
1. 新增 Rust TaskService；
2. 新增最小 Tauri commands；
3. 前端如需调用，必须通过 Frontend Service / callTauri；
4. 不允许在组件、store、composable 中直接 invoke；
5. 不替换现有 AI Provider 队列；
6. 不引入 Python 常驻服务；
7. 不做真实 AI 生成。
```
