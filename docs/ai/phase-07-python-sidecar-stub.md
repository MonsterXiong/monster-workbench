# Phase 07：Python sidecar stub

## 对 Codex 说

```text
请执行 docs/goals/goal_05_python_sidecar_stub.md。

要求：
1. 新增 Python 常驻服务原型；
2. 只实现 /health、/tasks stub、/events stub；
3. 支持 --port 和 --token；
4. 接口必须校验 token；
5. 不执行真实模型调用；
6. 不替换 ai_provider_tester.py；
7. 只能通过 Rust 调用。
```
