# Open Loops

## 2026-07-04 高清增强稳定性
- [ ] OpenAI-compatible adapter 已禁止把 `upscale_2x` / `upscale_4x` 当作稳定能力；后续只有接入明确上传源图并通过真实 provider smoke 的高清 adapter/endpoint 后，才重新开放图片工作台“提高清晰度”入口。smoke 需继续校验源图被作为图像输入使用（例如 `image_tokens > 0` 或 provider 等价证据），避免文本重画误报成功。

本文只保留当前仍未闭环、且会影响后续推进的事项。已移除的旧独立创作工作台、批量任务、任务队列、资产库、常驻运行时和历史执行包事项不再记录。

## AI 工作台

- [x] 图片工作台 `/image-workbench` 已接入 schema / repo / command / service / store / route，并补齐历史作业、最近资产库、模板 CRUD、收藏、取消 job、重试失败任务、详情审计和浏览器/mock 文生图工作台闭环；资产路径白名单、data URL 禁止入库、任务状态机和 asset/metadata/model_run 事务写入已加固。真实测试已覆盖应用启动、Provider 配置探测、真实 DB 表存在性、取消/失败路径、重启后 running/queued 孤儿任务恢复、失败 model_run 审计，以及真实 Provider 成功生成后经 `ImageWorkbenchService` 复制到 `~/.monster-tools/ai/image-workbench/assets` 并写入真实 DB 的 asset/metadata/model_run 成功链路。
- [x] AI 业务生成已从页面直连收敛为 `aiService.runBusinessGenerationTask()` / 后端 runner：前端业务只传 `providerConfigId`、prompt、model、options 和 requestId，不携带完整 provider config；旧独立对话、生图和能力测试页面与会话运行时已移除，generation task 仅作为后端业务通道、Provider 诊断队列同步和图片/分镜工作台底座保留。
- [x] 2026-06-14 已把 active model config 从旧 `chat/image` 扩展为 capability binding 缓存：`chat/image/txt2img/img2img/inpaint/upscale_2x/upscale_4x/person_consistency/audio/video` 均可绑定模型配置；Rust 业务生成解析 `config.json` active binding 时按文件修改时间和长度缓存，配置保存后自动失效重读；`/ai` 配置页维护模型配置与能力绑定，`/image-workbench` 使用 `txt2img` binding，业务取消前端语义统一为 `aiService.cancelGeneration(requestId)`。
- [x] 2026-06-14 图片工作台文生图/重试入口改为 Rust 后端异步 job runner：先创建或恢复持久 job/task 快照并立即返回，`start_image_workbench_job_runner` 在后端通过 DB task claim/lease 推进 task；Tauri 启动与页面初始化都会把未完成任务恢复为可领取状态并启动 runner，结果仍只通过 `artifact.path` 写入受控资产库和 model_run 审计，不把大图内容放入 Pinia 状态树。
- [x] 2026-06-14 新增后端 generation task registry 语义层：`generate_ai_content` / `generate_ai_business_content` 会登记轻量任务快照，新增 `enqueue_ai_business_generation`、`get_ai_generation_task`、`list_ai_generation_tasks`、`cancel_ai_generation_task`；前端 `aiService.cancelGeneration()` 改走 generation 取消命令，`ai-provider-queue-sync` 会同步 generation task 到前端队列，图片工作台则由 Rust 后端 runner 直接调用 `run_business_generation_blocking()` 并写入自身 asset/metadata/model_run。浏览器图片工作台 mock 也通过 AI Provider mock 的 generation task 推进，尽量贴近桌面端真实路径。
- [x] 2026-06-14 通用业务 generation task 新增 `ai_generation_tasks` 持久任务表：业务请求只持久 `providerConfigId`、prompt、model、options 和 requestId，不持久完整 Provider config/API Key；Tauri 启动会恢复 queued/running 业务任务并重启后台生成，`get_ai_generation_task` / `list_ai_generation_tasks` 可从 DB 回读结果和 artifact metadata。
- [x] 2026-06-14 通用业务 generation task 恢复已有服务级闭环测试：`resume_persisted_business_generation_restarts_queued_task` 直接种入 `ai_generation_tasks` queued 任务，模拟重启后由 `resume_persisted_business_generations()` 重新启动后台生成并写回 success 与 artifact；完成/失败路径已调整为先写 DB、再发布内存 task 终态，避免 UI 看到完成但持久表仍停留在 running 的短窗口。
- [x] 2026-06-14 新增 `business_generation_preference_cache_invalidates_when_config_file_changes`，覆盖业务 active binding 缓存失效；`direct_generation_can_cancel_running_sidecar` / `direct_generation_can_cancel_queued_request` 已改走 `cancel_generation_task`，直接验证 generation 统一取消入口可中止 running/queued 生成请求。
- [x] 2026-06-14 AI generation 稳定性继续加固：图片 pending message 首次持久化即携带 `requestId`，避免刷新/取消时短暂丢失请求 ID；内存 generation registry 和 `ai_generation_tasks` repo 均保持终态幂等，`complete` 不再覆盖 `success/failed/canceled`；service 层会先尊重已持久化终态，再决定是否进入内存队列和 Provider queue，防止重启后同 `requestId` 复活已取消/已完成任务；取消兜底覆盖尚未进入 Provider queue 的 memory-only queued task。
- [x] 2026-06-14 `/image-workbench` 按执行包复核补齐基础全量闭环：文生图批量生成、关键词扩写/默认负向约束、每任务 expanded prompt、元提示词复制、单张重新生成、风格继续、历史/收藏/模板、详情审计、单张图片导出、受控导出文件夹、参考图/外部图入口、外部图片近似反推提示词口径和浏览器打开路径降级提示已验证；Playwright mock 生成、详情、导出、参考图反推控制台 0 error / 0 warning。
- [ ] 图片工作台后端后续扩展前继续拆：repo 已拆出 `image_workbench_types.rs` 与 `image_workbench_repo_tests.rs`，service 已拆出 `image_workbench_asset_policy.rs`、`image_workbench_import.rs`、`image_workbench_failure.rs` 与 `image_workbench_cleanup.rs`；2026-06-19 增 `image_workbench_row_mapper.rs` / `image_workbench_task_transition.rs` / `image_workbench_runner.rs`；2026-06-20 把 repo 私有只读查询 helper 抽到 `image_workbench_query.rs`。后续增加更多受控导入、ZIP 打包或资产组语义前，再拆 mutations、asset recording builder。
- [x] 2026-06-20 图片工作台数据模型基础层已落地：`image_workbench_groups` 表（`job_id`/`source_id`/`name`/`type`/`agent_preset`/`agent_ids_json`/`base_prompt`/`count`/时间戳 + FK CASCADE + `idx_groups_job`）已建；`image_workbench_tasks` 补 `group_id`/`variant_index`/`failure_type`/`failure_hint` 与 `(job_id, group_id, variant_index)` 索引；`image_workbench_assets` 补 `group_id`/`rating`（与 favorite 并存）/`parent_asset_id`/`root_asset_id`/`version_index` 与 `idx_assets_group`；迁移走幂等 `ensure_*_columns`。repo 新增 `query_assets`（group_id/min_rating 筛选 + 分页 + 排序，`list_recent_assets` 委托其默认条件）、`set_asset_rating`、`create_group`/`list_groups`/`get_group_by_id`，均由 repo 单测覆盖。
- [x] 2026-06-20 图片工作台 grouped job + 版本链已真正写入（契约零变更）：`repo.create_job` 在同事务内建一个资产组（`source_asset_id` 有值→`type=rerun`，否则 `fresh`；`base_prompt=prompt`、`count=quantity`），该 job 的 N 个 task 全挂这个组并写 `variant_index=0..N`；`repo.record_asset` 经 `image_workbench_query::resolve_lineage` 从 `job.source_asset_id` 派生版本链——`parent=源asset.id`、`root=源asset.root??源asset.id`（链根向上收敛）、`version=源asset.version+1`，`group_id` 跟随 task；显式传入值（测试/导入）优先于派生值。`CreateImageWorkbenchJobRequest`/`NewImageWorkbenchJob`/runner/Python 均未改。
- [x] 2026-06-22 图片工作台前端推断已下沉为读后端字段：`ImageWorkbenchAsset` 加 `groupId`/`rating`/`parentAssetId`/`rootAssetId`/`versionIndex`，`ImageWorkbenchTask` 加 `groupId`/`variantIndex`/`failureType`/`failureHint`（均为兼容增量可选字段，`toAssetCard` 自动透传）；mock 同步建 group + 派生版本链（与后端 `resolve_lineage` 一致），删除 job 级联清组；`imageWorkbenchReview.ts` 删除 `resolveRootAssetId` 回溯循环，`resolveLineageRole`/`resolveDeliveryCandidate`/`buildLineageFamilyAssets`/`buildVersionChain` 改读 `rootAssetId`/`parentAssetId`/`versionIndex`，移除 jobById 绕行。聚族用 `rootAssetId`（整条编辑链），`groupId`（一 job 内变体）仅作为字段透传，未引入新 gallery 视图；delivery 判定入口不变。
- [x] 2026-07-02 图片工作台评分与资产组读取入口已从 repo 贯通到 command / service / mock / 前端：新增 `query_image_workbench_assets`、`list_image_workbench_groups`、`set_image_workbench_asset_rating`，前端 service/types 已同步，浏览器 mock 拆出 `image-workbench-assets.mock.ts` 避免主 mock 继续膨胀；右侧选中资产的“交付与整理”区新增 0-5 评分控件，详情区显示评分。已验证 `npm run verify`、`cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench`、touched Rust 文件 `rustfmt --check`、`git diff --check`，并用 Playwright CLI 浏览器 mock 跑通生成 4 张图后设置 4 分。
- [x] 2026-07-02 图片工作台 `failure_type` / `failure_hint` 已从空字段推进到业务写入闭环：service 对失败/取消状态写入结构化失败类型与短提示，支持显式传入并按错误文本兜底推断 `auth/model/size/rate_limit/timeout/connection/save/cancelled/unknown`；repo 在 retry/recover/claim/janitor 回滚时清空旧失败字段，卡死任务标 `timeout`，取消标 `cancelled`；任务列表显示失败类型标签和 hint。
- [x] 2026-07-02 图片工作台“交付”已从前端推断下沉为正式资产字段：`image_workbench_assets.delivery_status` 可空，当前语义为 `ready`（已收藏、处于版本链中、且没有子分支的叶子资产）；schema 启动时对旧数据幂等回填/纠偏，repo 在收藏切换和新分支资产入库时维护该字段，export metadata 带出 `deliveryStatus`；前端图库分组和卡片徽标改读 `asset.deliveryStatus`，浏览器 mock 保持同等规则。已验证 `npm run verify`、`npm run check:architecture`、`cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench`、touched Rust 文件 `rustfmt --check`、`git diff --check`，并用 Playwright CLI 浏览器 mock 跑通生成、风格继续、收藏叶子资产后显示“可交付结果 / 交付 1”。
- [x] 2026-07-02 图片工作台作业生命周期已补 `archived_at_ms` / `deleted_at_ms`：schema/job DTO/mock 同步字段，默认 job 列表和资产查询只返回未归档、未删除作业；`delete_job` 改为 DB 软删除并取消未完成 task，snapshot 仍可按 job id 读取以便审计、恢复和文件清理。已验证 `cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench`、`npm run verify`、`npm run check:architecture`、touched Rust 文件 `rustfmt --check`、`git diff --check`，并用 Playwright CLI 浏览器 mock 确认删除后历史/当前结果/资产库默认视图均归零且控制台 0 warning / 0 error。
- [x] 2026-07-02 图片工作台资产文件健康状态已落地：`image_workbench_assets` 新增 `integrity_status` / `integrity_error` / `integrity_checked_at_ms`，record asset 默认 `ok`，service 在 `get_snapshot` / `list_assets` / `query_assets` 读取展示范围时自动检查受控文件路径，缺文件标 `missing`，空文件/非文件/metadata 读取失败标 `corrupt`；导出 metadata、前端类型、图库异常徽标和详情“文件状态”已同步。已验证 `cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench`、`npm run verify`、`npm run check:architecture`、touched Rust 文件 `rustfmt --check`、`git diff --check`，并用 Playwright CLI 浏览器 mock 跑通生成 4 张图后详情显示“文件状态 / 文件正常”，控制台 0 warning / 0 error。
- [x] 2026-06-19 图片工作台运行时边界沉淀为后端规则与代码：并发上限前端只传请求值，`AiProviderTestQueue` 按 Provider 维度 clamp，`runtime_dispatcher::GLOBAL_WORKER_LIMIT` 在进程级 4 个 worker permit 上 gate；runner 用 `claim/lease + worker_id` CAS 保证多 worker 不重复；单任务取消仅 queued，running 经 `ai_provider_cancel_registry` 翻 cancel_token；单任务重试保留历史 attempt/model_run 并自增 retry_count；失败枚举固定为 `auth/model/size/rate_limit/timeout/connection/save/cancelled/unknown` 由后端返回。事实沉淀进 `docs/architecture-current-state.md` §6。
- [x] 2026-07-02 图片工作台前端热点已完成一轮可验收拆分：`ImageWorkbenchPage.vue` 拆出 `ImageWorkbenchHistoryPanel.vue`、`ImageWorkbenchTaskPanel.vue`、`ImageWorkbenchCompareStrip.vue`、`ImageWorkbenchInspector.vue`，页面降到体量预算内；`imageWorkbenchReview.ts` 同步修复字面量类型收窄。该轮未触碰 Tauri/IPC/HTTP/DB 边界，后续新增复杂能力前再继续拆 Composer、AssetBoard、版本链和 Pinia runtime/planner。
- [x] 2026-07-02 generated-assets 扫描导入器已从后端接到页面入口：新增 `import_image_workbench_generated_assets` command / service / TS service/types，用户显式传入目录后只扫描同级 `.json/.png` 成对文件；旧 schema 可导入中文 prompt/model/size，新 schema 保留 `groupId/groupName/groupType/sourceId/agent` 到 `image_workbench_groups`；PNG 会复制进受控资产目录，asset 通过 `import_fingerprint` / `import_source_path` 幂等去重；缺图返回 `missing`，缺 JSON / JSON 解析失败导入图片但标 `integrity_status=corrupt`，不静默跳过。`/image-workbench` 顶部已接“导入生成资产”按钮，目录选择走 `systemStore.selectPath("folder")`，导入后刷新历史/资产并在有 `jobId` 时切到导入作业，浏览器 mock smoke 确认按钮可见且点击无 console error。已验证 `cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench`（50 passed）、`npm run verify`、`npm run check:architecture`、`git diff --check`。后续仍需真实 Tauri 窗口目录对话框/观感复核和 ZIP 打包策略。
- [x] 2026-07-02 generated-assets 真实 app-data 导入 smoke 已完成：先备份真实库到 `C:\Users\刘雄成\AppData\Roaming\com.monster.tools\monster_workbench.20260702-181643.codex-import-smoke.bak`，再用临时 env-gated Rust 测试调用正式 `import_generated_assets`，对 `C:\Users\刘雄成\Documents\New project 2\generated-assets` 前 8 对样例写入真实 DB 与 `~/.monster-tools/ai/image-workbench/assets`；第一次结果为 job `iw-job-1782987429560-1`、8 imported、0 failed，第二次同范围 8 duplicates、0 failed，资产任务目录从 16 增至 24。临时测试已移除；真实窗口当前仍因离屏/坐标绑定不稳定，未覆盖系统目录选择对话框点击流。
- [x] 2026-07-02 图片工作台用户文案与图片预览体验已收敛一轮：顶部、创作设置、审片区、参考图、局部重绘、图库分组、详情和错误提示改成面向用户的短文案，减少 `契约`、`Provider`、`Job/Task`、`mask` 等内部表达；新增私有组件 `ImageWorkbenchLightbox.vue`，中间图库、对比条、右侧预览、版本链和相关图点击图片可放大查看，点击背景或“关闭”返回。已验证 `npm run verify`、`git diff --check`，并用 Playwright CLI 浏览器 mock 生成 4 张图后确认点图弹层可打开/关闭，控制台 0 error / 0 warning。
- [x] 2026-07-02 图片工作台“风格继续”横跳已修复：将 job 快照轮询拆到 `image-workbench-polling.ts`，用轮询 token 和 `activeJobId` 拦截旧任务继续写回 `currentSnapshot`、`selectedJobId` 与 loading 状态，避免新旧作业来回抢当前页面；同时清理旧版/新版续写后缀，连续点击“风格继续”不会让关键词递归变长。已验证 `npm run verify`、`git diff --check`，并用 Playwright CLI 浏览器 mock 跑通生成、选图、连续风格继续，新任务稳定停留且续写后缀保持 1 段，控制台无 error/warning。
- [x] 2026-07-02 图片工作台创作入口与参考图体验已按用户反馈收敛：右上角无意义操作已移除，生成/取消/导入/刷新回到左侧创作区；上传参考图后可点“人物表情”切到人物保持并自动填入短提示；`img2img` / `person_consistency` 构建上下文时优先使用上传参考图，避免被当前选中资产覆盖；右侧“同组图片”去重后只显示一组相关图，避免重复展示。已验证 `npm run verify`、`git diff --check`、`cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench`、`cargo test --manifest-path .\src-tauri\Cargo.toml ai_service_generation_tests` 和 Playwright CLI 浏览器 mock。
- [x] 2026-07-02 图片工作台资产库选图闪烁已修复：后台生成轮询不再无条件写回 `currentSnapshot/selectedJobId`，只有用户仍查看该生成作业时才刷新主画面；用户切到历史作业或资产库旧图时，生成任务继续后台收敛但不抢视图。资产库主分组取消随选中图动态重排的“当前链路”分组，选图只更新选中态和右侧详情，避免点击尺寸文字后缩略图重新洗牌。左侧“历史作业 / 模板”补充短说明并支持侧栏滚动，历史/模板条目改为两行显示和 title 提示。已验证 `npm run verify`、`npm run typecheck`、`git diff --check`，并用 Playwright CLI 浏览器 mock 跑通生成两批、切资产库、点击尺寸区域选旧图，控制台 0 error / 0 warning。
- [x] 2026-07-02 图片工作台模板、回到当前生成和右侧下一步区域已继续收敛：模板从左侧侧栏移到创作区“模板”快捷弹出层，只负责保存/套用常用提示词，保存成功后自动收起；390px 窄宽度下修复单列面板仍按桌面滚动区收缩导致模板按钮被历史区拦截的问题。查看历史作业时显示“回到最新生成”，可从旧作业切回最新批次；右侧“下一步”从大卡片改为紧凑动作行。OpenAI-compatible adapter 已为声明原生能力的 `img2img/person_consistency/inpaint` 增加 `/images/edits` multipart 上传路径，支持 reference/source/mask 文件上传；未声明原生能力时仍走原有文生图降级。已验证 `npm run verify`、`npm run typecheck`、`npm run test:ai-sidecar`、`git diff --check` 和 Playwright CLI 浏览器 mock，控制台 0 error。
- [x] 2026-07-02 图片工作台 1-4 集中收口已完成：当前 diff 边界、热点体量和旧文档引用已核对；前端页面热点已拆分并清理无效膨胀；OpenAI-compatible `person_consistency` 新增 `/images/edits` multipart reference 上传测试；浏览器交互 smoke 覆盖生成、资产库切换、模板弹层、点图放大和 390px 窄宽度，真实 Tauri 窗口已截图确认 `/image-workbench` 主界面可见。通过 `npm run typecheck`、`npm run check:architecture`、`npm run verify`、`npm run test:ai-sidecar`、`cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench` 和 `git diff --check`。
- [x] 2026-07-02 图片工作台已补受控“清理文件”闭环：删除作业后可清理其位于 `~/.monster-tools/ai/image-workbench/assets` 受控目录内的本地图片文件，保留 DB 历史与审计；非受控路径、缺失文件和异常会跳过并返回统计。UI 在左侧提供“清理文件”入口，浏览器 mock smoke 覆盖生成 4 张图、删除作业、确认清理并看到删除统计。已验证 `npm run verify`、`npm run check:architecture`、`cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench`（51 passed）和 `git diff --check`。ZIP 打包暂不加入，避免违反 Rust 依赖最小化策略。
- [x] 2026-07-02 图片工作台生成数量已移除旧 16 张硬上限：后端 contract 返回 `max_quantity: None`，创建任务只拒绝 `quantity == 0`；前端数量输入不再设置最大值，store/mock 不再把数量 clamp 到 16；repo 按任务序号生成默认提示词，避免高数量任务先构造大 prompt 数组。已验证 `npm run typecheck`、`npm run check:architecture`、`npm run verify`、`cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench`（52 passed）、touched Rust 文件 `rustfmt --check` 和 `git diff --check`。后续若用户一次生成大量图片，需要继续优化队列展示、分批确认和磁盘/费用提示，但产品/接口层不再设 16 张硬限制。
- [x] 2026-07-02 图片工作台按 1-4 产品化继续收口：大批量生成新增 32 张起的确认弹窗和左侧短提示，任务面板新增全部/进行中/失败/已结束筛选并默认只渲染前 120 条；参考图模式会提示“原生接收参考图”或“按关键词模拟”，避免误以为所有 Provider 都能稳定人物一致；中间图库改按真实 `image_workbench_groups` 分区，右侧详情显示图片组与版本号，同组图片按 `groupId` 聚合；参考图系统选择对话框加图片格式过滤，“清理文件”改用项目统一确认弹窗。已验证 `npm run verify`、`npm run test:ai-sidecar`、`cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench`（52 passed）、`git diff --check`，并用 `npx playwright screenshot` 复核 1365x900 页面；Playwright CLI snapshot/run-code 本轮在当前会话卡住，未作为交互 smoke 依据。
- [x] 2026-07-03 图片工作台版面继续收紧：移除页面内部顶部标题栏和底部统计栏，避免与应用外层导航/标题重复；三栏内容区不再额外加 `px-3`，错误/提示条也取消额外横向 margin，左右边界跟其他页面保持一致并释放更多审片空间。已验证 `npm run typecheck`、`npm run check:architecture`、`npm run verify`、`git diff --check`，并用 `npx playwright screenshot` 复核 1365x900 页面。
- [ ] 图片工作台升级阶段验收门禁：`npm run check:architecture`、`npm run typecheck`、`cargo test image_workbench`；改 Rust/capabilities/打包链路时跑 `npm run tauri:build:no-bundle`；测试需覆盖 grouped job 入库、多 worker 并发 claim 不重复、queued 单条取消、failed 单条重试、rating 刷新仍存、复跑产生 parent/root/version、asset list 分页/筛选/排序、generated-assets 新旧 metadata 导入兼容；真实窗口验收三栏布局/队列折叠/Composer 不遮挡/modal 键盘操作。
- [ ] Python Provider 新增非 OpenAI-compatible 协议前，继续从 `provider_adapters.py` 拆新的 adapter family；新增 action 时只扩展 `ai_provider_actions.py` 与 `ai_provider_results.py`，不要回填到 `ai_provider_tester.py` 主入口。
- [x] 2026-07-03 已新增真实 Provider 图片编辑 smoke 入口：`npm run smoke:real-image-edit -- --capability person_consistency --reference-image <path>`，脚本默认读取 `~/.monster-tools/config.json`，必须显式设置 `MONSTER_REAL_PROVIDER_SMOKE=1` 才会发起真实请求；支持 `img2img/person_consistency/inpaint/all`，可用 `--allow-temp-capability` 临时验证 OpenAI-compatible `/images/edits` multipart 上传合同，输出只写脱敏 request/result/report 到 `output/real-provider-image-edit-smoke/`。已验证脚本语法、安全门禁、`npm run test:ai-sidecar`、`npm run verify` 和 `git diff --check`。
- [x] 2026-07-03 已按优先级继续收口图片工作台核心问题：`smoke:real-image-edit --list-configs` 与 preflight 通过；当前 `gpt-image-2` 配置已通过模型名隐式识别为支持原生 `img2img/inpaint/person_consistency`，不再需要仅靠手动勾选避免降级；“继续同一风格”改为基于选中图走 `img2img`，跨作业/资产库选图会把目标 asset id 一次性传给 `selectJob`，减少先选错图再跳转的闪烁；右侧原始生成返回折叠为“生成记录”，资产组/链路/交付摘要继续读真实字段；旧 `/ai` 图片入口、Provider 测试链路和 sidecar 也移除 1-4 张旧硬上限，只保留数量必须大于 0。已验证 `npm run verify`、`npm run test:ai-sidecar`、`cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench`、`cargo test --manifest-path .\src-tauri\Cargo.toml ai_service_generation`、`cargo test --manifest-path .\src-tauri\Cargo.toml ai_provider_config`、`git diff --check`。
- [x] 2026-07-03 图片工作台主内容区已明确为“产物优先”：中间区移除大统计块，改为轻量对比条 + 生成结果/资产库；右侧切换“任务/详情”，生成时看队列状态，选中图片时看资产属性、版本链和后续动作。任务队列增加进度、运行/失败/已结束摘要、筛选和排序，避免继续堆无意义按钮。已验证 `npm run typecheck`、`npm run check:architecture`、`npm run verify`、`git diff --check`，并用浏览器 mock 跑通生成 4 张图、切详情和点图放大，控制台 0 error / 0 warning。
- [x] 2026-07-03 图片工作台继续减少“迷路”路径：中间图库头部新增当前查看上下文条，明确当前生成/历史作业/资产库与图片数量；生成中且右侧停在详情时可一键回任务，查看历史作业会自动回到当前结果视图；右侧任务队列的已完成任务新增“查看结果”，可直接定位对应产物并切到详情。已验证 `npm run typecheck`、`npm run verify`、`git diff --check`，并用 Playwright CLI 浏览器 mock 跑通生成 4 张图、任务跳结果、资产库上下文切换；截图保存到 `output/playwright/image-workbench-context-queue.png`。
- [x] 2026-07-03 修复图片工作台真实窗口图片不显示的回归：Tauri CSP 的 `img-src` / `media-src` 已补 `https://asset.localhost`，覆盖 `convertFileSrc` 可能生成的 asset host；参考图、局部重绘底图、图库、对比条、右侧详情和大图弹层均接入图片加载失败回退，占位图 helper 下沉到 `src/utils/image-placeholder.ts`，避免页面直接导入 service 触发分层违规。已验证 `npm run check:architecture`、`npm run typecheck`、`npm run verify`、`git diff --check`、`npm run tauri:build:no-bundle`。用户当前已打开的 Tauri 窗口需要重启后才能吃到新的 CSP。
- [x] 2026-07-03 继续修复真实窗口仍显示占位图：真实 DB 中最新导入作业 `iw-job-1783044697827-1571` 的 87 个资产均为 `missing`，会正确显示占位；同时现存可用资产路径普遍带 Windows `\\?\` 扩展前缀，Tauri asset scope / `convertFileSrc` 不应直接消费该格式。前端显示前会剥离 `\\?\` / `\\?\UNC\`，后端 `ImageWorkbenchAssetPathPolicy` 后续入库也会保存普通 Windows 路径；初始进入图片工作台时优先打开进行中作业，其次打开最近有可用图片的作业，避免全缺失导入记录占据首屏。已验证 `npm run verify`、`cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench`、touched Rust 文件 `rustfmt --check`、`git diff --check`、`npm run tauri:build:no-bundle`。
- [x] 2026-07-03 图片工作台资产库分页入口已补齐：确认“资产库 120”原本只是前端最近资产加载窗口，不是总数；现在资产库使用 `query_image_workbench_assets` 分页加载，tab 和上下文显示“已显示 {count}+”，底部可点“加载更多历史图片”继续查看旧图片。分页状态拆到 `src/stores/image-workbench-assets.ts`，主 store 未突破体量基线。已验证 `npm run typecheck`、`npm run check:architecture`、`git diff --check`。
- [x] 2026-07-03 图片工作台无效资产清理、历史位置、预览和并发生成已补齐：左侧新增“清空无效”，会扫描并移除 `missing/corrupt` 资产记录，解决真实文件被删后资产库长期显示占位的问题；历史作业改到右侧切换面板，左侧只保留生成操作；大图预览支持上一张/下一张、底部缩略轮播和点击背景关闭；后端同一作业可并发启动多个 worker，通过 task claim/lease 防止重复领取。已验证 `npm run typecheck`、`npm run check:architecture`、`cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench`（55 passed）、`cargo fmt --check`、`git diff --check`、`npm run tauri:build:no-bundle`。
- [x] 2026-07-03 图片工作台多 job 队列体验已补齐：前端不再用单个 `activeJobId` 锁住整个工作台，改为 `activeJobIds` 集合和每 job 独立轮询；已有任务运行时主按钮显示“加入队列”，可继续创建新 job，取消按当前可见 job 执行。后端新增单 job worker 上限 2，配合全局 worker 上限 4，支持单 job 内 task 并发并给另一个 job 留出并行空间。断线/重启恢复继续依赖持久 DB job/task、claim/lease、启动恢复和失败任务重试。已验证 `npm run verify`、`cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench`（55 passed）、`cargo fmt --check`、`git diff --check`、`npm run tauri:build:no-bundle`。
- [x] 2026-07-03 图片工作台 job/task 查看跳转已继续优化：右侧三段切换固定为“任务 / 详情 / 历史作业”，不再展示动态统计；任务面板顶部新增“运行队列”，直接列出运行中或排队中的 job，点击即可切换到对应任务列表，解决新 job 加入队列后上一批运行任务不易找的问题。已验证 `npm run verify`、`git diff --check` 和 Playwright 浏览器快照/截图，控制台 0 error / 0 warning。
- [x] 2026-07-03 图片工作台重试与右侧队列已继续收口：失败任务不再受旧 `maxRetries` 次数限制，达到旧上限后仍可再次重试；右侧以 job 队列为主，不再逐条展示 task 明细，当前 job 只展开进行中/失败/已结束摘要，具体图片、提示词和结果查看交给中间图片区与右侧详情。已验证 `npm run typecheck`、`npm run check:architecture`、`npm run verify`、`cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench`（55 passed）、`cargo fmt --check`、`git diff --check`、`npm run tauri:build:no-bundle`；本轮再次验证 `npm run verify`、`git diff --check`，并用 Playwright CLI 浏览器 mock 生成 4 张图确认右侧仅保留队列卡和摘要，控制台 0 error / 0 warning；截图保存到 `output/playwright/image-workbench-queue-primary.png`。
- [x] 2026-07-03 图片工作台选中态已改为显式选择：没有 `selectedAssetId` 时不再回退到第一张、资产库第一张或最新生成图；刷新、轮询、切作业和生成完成只保留仍真实存在的已选图片，目标资产跳转才通过 preferred asset id 一次性选中。生成结果会保持未选中，直到用户点击图片；对比条也不会用第一张伪造“已选”。已验证 `npm run typecheck`、`npm run verify`、`git diff --check`，并用浏览器 mock 生成 4 张图确认没有自动出现“已选”。
- [x] 2026-07-03 图片工作台队列“正在进行但没动静”已定位并补兜底：真实 DB 中两个 running job 实际没有 running task，剩余 9 个任务停在 `retrying` 且无 `claim_token/lease`，最近失败原因为远程 `gpt-image-2` 服务断开或未返回图片。根因是重试命令只把失败任务标为 `retrying`，runner 启动依赖前端第二步调用，页面刷新/状态中断时会留下空转。现已改为 `retry_image_workbench_failed_tasks` 后端命令直接启动 runner，前端不再重复启动；右侧摘要拆出“等待/生成/重试/失败/结束”，避免把 `retrying` 伪装成“进行中”。已验证 `cargo fmt --check`、`npm run typecheck`、`npm run check:architecture`、`npm run verify`、`cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench`、`git diff --check` 和 `npm run tauri:build:no-bundle`。当前已打开的旧 Tauri 窗口仍需点击左侧“刷新”触发恢复，或重启窗口后吃到新后端兜底。
- [x] 2026-07-03 已处理真实窗口动态导入报错：旧 Tauri dev 窗口仍请求 `localhost:1420`，但 Vite dev server 已退出，导致 `AiPage.vue` / `UtilsDocsPage.vue` 懒加载失败；已关闭旧 `monster-tools`/cargo 进程并重新启动 `npm run tauri:dev`，18:12 后应用日志无新增前端异常。同时修复重试后 job 回到 `running` 仍保留旧 `finished_at_ms` 的数据一致性问题，避免队列把运行中作业误读为已完成。已验证 `cargo fmt --check`、`cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench`（56 passed）、`git diff --check`；当前 50 张 job 为真实运行：4 个 running task 有心跳，2 个 retrying 等待重试。
- [x] 2026-07-03 图片工作台队列卡片继续收敛：状态摘要只展示非零且需要关注的等待/进行/重试/失败，完成数量交给进度条表达；当当前作业有失败任务时，队列卡片下直接展示最近失败原因，并可展开查看最多 5 条完整失败原因。已验证 `npm run typecheck`、`git diff --check`。
- [x] 2026-07-03 图片工作台已识别并产品化展示 Provider 账号池不可用错误：`HTTP 503 {"message":"No available compatible accounts"}` 表示网关可达但请求模型没有可用兼容账号，不是本地队列卡住；sidecar 归类为 `provider_unavailable`，工作台任务失败类型显示“账号不可用”，失败提示收敛为“当前模型没有可用账号或账号池已耗尽，请换一个模型/Provider，或稍后重试。”已验证 `npm run typecheck`、`npm run check:architecture`、Python sidecar 全量 contract、`cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench` 和 `git diff --check`。
- [x] 2026-07-03 图片工作台参考图选择已支持资产库多选：左侧参考图区可进入“资产库”选择模式，点击图片会加入/移除参考集，已选多张会在左侧以缩略列表展示并可逐张移除；右侧详情也可把当前选中图加入参考集。生成时保留第一张作为主 `sourceAssetId/sourceImagePath` 以兼容版本链，同时把所有参考资产解析为本地 `referenceImagePaths`，OpenAI-compatible `/images/edits` 会逐张上传为官方 `image[]` 文件字段。已验证 `npm run typecheck`、`npm run check:architecture`、`npm run test:ai-sidecar`、`cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench` 和 `git diff --check`。
- [x] 2026-07-03 `gpt-image-2` 图生图/人物保持真实链路已验证：OpenAI-compatible adapter 对 `gpt-image-*` 自动识别 `img2img/person_consistency/inpaint` 原生编辑能力，真实请求走 `/images/edits` multipart 并使用官方 `image[]` 图片字段，避免前端把参考图任务降级成纯文生图；`smoke:real-image-edit` 已用 `ai-model-1780916920189-5krffy` / `localhost:4444` 跑通 `img2img` 和 `person_consistency`，返回结果包含 `input_tokens_details.image_tokens`，并各保存 1 张真实图片。本轮 sidecar contract 进一步覆盖同一次 img2img 上传多张 `image[]` 参考图。
- [x] 2026-07-03 `gpt-image-2` 局部重绘真实链路已验证并修正 mask 协议：工作台后端 `save_mask` 改为按源资产真实尺寸生成 PNG alpha mask，不再保存 SVG；Sidecar 对 mask 增加 PNG 校验；`smoke:real-image-edit --capability inpaint --auto-mask` 已真实跑通并返回 `input_tokens_details.image_tokens`。第一次灰度 mask 失败原因为 `invalid_mask_image_format / mask size does not match image size`，修正为 alpha mask 后成功保存 2 张结果。
- [x] 2026-07-03 图片编辑结果产品策略已落地：Provider 在 `count=1` 时返回多张图片会全部落库为同一 task / 同一组下的候选图，模型调用审计只记录一次；`gpt-image-2` 编辑请求按参考图比例返回非请求尺寸时按真实图片元数据展示，右侧详情明确显示“图片尺寸”，不把尺寸差异当成失败。
- [x] 2026-07-04 `gpt-image-2` 输出格式与真实尺寸读取已补齐：sidecar 保存 WebP/JPEG/PNG 时按请求格式落扩展名和 MIME，WebP 已支持 VP8X/VP8/VP8L 头部尺寸解析；真实 `img2img` 使用双参考图、`output_format=webp`、`quality=medium` 跑通，返回 `image/webp`、实际尺寸 `1448x1086`，`actualImageSize` 不再误用请求尺寸。
- [x] 2026-07-04 `gpt-image-2` 清晰度增强边界已验证：真实 `img2img` 请求 `2896x2160` 高清重绘成功返回 WebP，但远程服务实际输出仍为 `1448x1086`，说明当前 Provider 可做参考图细节重绘但不能承诺 2x/4x 尺寸放大；图片工作台已把非原生 upscale 的入口收敛为“增强清晰度”，4x 只在 Provider 显式支持 `upscale_4x` 时展示。
- [x] 2026-07-03 图生图人物手部异常已基于真实结果复盘并补生成前约束：真实 DB 中 `iw-job-1783093008283-102` 的 `img2img` 多参考任务成功走 `gpt-image-2` / `/images/edits`，响应含 `image_tokens`，说明参考图已送达；结果问题集中在人物、道具、场景三参考图角色混淆，以及手持复杂道具导致手部/道具边界融合。后端任务 prompt 编译已加入参考图角色顺序约束、人物一致性主参考约束和手部结构约束，默认负向词补齐多余手、融合手部、断指、错误握持等风险词，并迁入 prompt 模块避免 service 继续膨胀。已验证 `cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench`、`npm run check:architecture`、`cargo fmt --manifest-path .\src-tauri\Cargo.toml --check`、`npm run typecheck`、`git diff --check`。
- [x] 2026-07-04 图片工作台提示词输入已支持显式引用参考图：左侧提示词下方按已选参考图显示“参考图1的人物 / 参考图2的道具 / 参考图3的场景 / 参考图4的风格”快捷按钮，点击会插入到光标位置；参考图卡片同步显示默认角色并提供“引用”按钮，帮助用户写的参考图序号和真实上传顺序对齐。浏览器 smoke 已验证上传参考图后可插入“参考图1的人物”，截图在 `output/playwright/image-workbench-reference-insert-fixed.png`。
- [x] 2026-07-04 图片工作台参考图角色已从默认推断升级为可编辑上下文：每张参考图卡片可选择人物 / 道具 / 场景 / 风格，引用按钮和插入提示词会跟随角色变化；生成 job 时把 `referenceRoles` 写入 `personContextJson`，后端 prompt 编译优先读取该角色数组，为每张参考图生成“只用于人物/道具/场景/风格”的内部约束，`img2img` 和 `person_consistency` 都可复用。浏览器 smoke 已验证角色从人物改为道具后，快捷引用变为“参考图1的道具”；后端 `image_workbench_prompt` 测试覆盖显式角色解析。
- [x] 2026-07-04 图片工作台已补结果质检标记和一键“修手”入口：右侧选中图可标记手部异常 / 人物不像 / 道具错 / 场景错；点“修手”会自动切到局部重绘、填入只修蒙版手部的提示词、补手部专用负向词、数量收敛为 1，并进入蒙版绘制，不直接开跑生成。已验证 `npm run typecheck`、`npm run check:architecture`、`git diff --check`，并用 Playwright 浏览器 mock 确认质检区可见、`hands` 标记可选、修手后进入局部重绘画布。
- [x] 2026-07-04 “修手”真实 Provider 局部重绘已验证：新增 `smoke-real-provider-image-edit.py --mask-rect x,y,w,h`，支持用比例/像素矩形生成手部 alpha mask；使用真实工作台人像资产、`gpt-image-2` / `localhost:4444` 跑通两次 inpaint，均返回 HTTP 200、各保存 1 张 PNG，结果包含 `input_tokens_details.image_tokens`。复盘结论：较宽 mask 会明显重绘手势和附近脸颊；收紧提示为“只修 masked hand anatomy”后更稳，但模型仍可能改变手势形状；推荐修手 mask 控制在手和袖口范围，尽量少覆盖嘴唇、鼻翼和大面积脸颊。对比图保存到 `output/real-provider-image-edit-smoke/fix-hands-real-tight-prompt/fix-hands-comparison.png`。
- [x] 2026-07-04 图片工作台质检标签已从前端状态下沉到 DB：`image_workbench_assets.quality_issues_json` 通过 schema / repo / service / command / TS service / mock 全链路读写，右侧质检标签刷新后仍随资产快照返回；右侧固定“修手”按钮改为“按标签修复”，按手部 / 人物 / 道具 / 场景优先级分别进入局部重绘、人物保持或图生图二次修复。已验证 `npm run typecheck`、`npm run check:architecture`、`cargo test image_workbench` 和 `git diff --check`。
- [x] 2026-07-04 按质检标签二次修复已做真实 Provider 复盘：使用真实工作台人像 + 法杖素材跑通 `person_consistency`、`img2img`、`inpaint` 三条最小请求，均返回 HTTP 200 且保存 1 张结果；人物保持结果稳定保留身份，证明“人物不像”可走 `person_consistency`，前端已进一步收紧为优先用选中图的根图/父图作人物身份参考、当前图作构图场景参考；道具局部重绘能收敛到被涂抹区域，但 mask 过宽会改道具形状和握持细节；场景直接 `img2img` 会连人物姿势和道具位置一起重绘，因此“场景错”已改为局部重绘入口，提示用户先涂背景/环境区域。结果保存在 `output/real-provider-image-edit-smoke/quality-*-real/`。
- [x] 2026-07-04 图片工作台队列卡死与局部重绘落库已修复：Provider 图片/视频排队从无限等待改为按请求超时窗口自动失败；worker 领取 task 后，参考图解析和结果资产落库失败都会写回 failed / failure_hint，不再留下 running；局部重绘 `maskPath` 允许工作台受控 `ai/image-workbench/masks` 目录，避免合法蒙版在结果落库时被拒绝。真实 DB 旧 running 任务已收口为 failed 且可见失败原因；已验证 `cargo test image_workbench`、`cargo test ai_provider_queue`、`npm run typecheck`、`npm run check:architecture`、`npm run tauri:build:no-bundle` 和 `git diff --check`。
- [x] 2026-07-04 图片工作台局部编辑体验已完成本轮增强：局部重绘画布拆成独立编辑器，支持 4x 局部缩放、撤销/重做、局部放大、原图/蒙版/叠加预览和保存后继续生成；对比条在选中局部重绘结果后自动展示“原图 / 当前图”，右侧版本链显示来源图到当前结果。补充修复：图库和对比条点击图片会先选中再放大，避免右侧详情停留在未选中。已用 Playwright 浏览器 mock 跑通生成 4 张图、进入局部修改、涂抹 mask、保存区域、提交局部重绘、队列 4/4 完成、选中候选后出现前后对比；截图保存在 `output/playwright/image-workbench-mask-preview.png` 和 `output/playwright/image-workbench-before-after.png`。已验证 `npm run typecheck`、`npm run check:architecture` 和 `git diff --check`。
- [x] 2026-07-04 按标签局部修复的尺寸协议已修正：真实失败 job 显示源图/mask 为 `1024x1536`，但旧请求沿用左侧残留尺寸 `1152x2048`，导致 `gpt-image-2` `/images/edits` 上游返回 HTTP 400 server_error。前端进入局部重绘/按标签局部修复时同步为选中图真实尺寸，并在尺寸下拉里动态补当前图尺寸；后端 worker 也会在 inpaint 执行前按源资产 width/height 覆盖生成请求尺寸，旧失败任务点“重试失败项”也能走修正后的尺寸。已验证 `npm run typecheck`、`npm run check:architecture`、`cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench`、`cargo fmt --check --manifest-path .\src-tauri\Cargo.toml` 和 `git diff --check`。
- [x] 2026-07-04 图片工作台本轮审查优化已完成：清理 `imageWorkbenchReview.ts` 中版本链/徽标旧兼容入参，图库会把当前选中图所在组或资产库选中上下文前置并避免重复分区；右侧队列改为短状态标签，失败原因会优先从 Provider JSON 中提取可读摘要并保留原始详情。同步复核真实 Provider 协议链路：资产库多参考图、上传多参考图、`gpt-image-2` 原生多图 `img2img` 和 `inpaint` 源图+mask 定向测试均通过。
- [x] 2026-07-04 图片工作台中间审图上下文已补齐：选中图片后，图库区域会显示“当前审图”摘要，包含本组数量、链路、分支和交付状态，并用可点击缩略图串起来源 / 当前 / 分支路径；浏览器 mock 已跑通生成 4 张图、选中首图、右侧详情同步和桌面截图复核，截图保存到 `output/playwright/image-workbench-focus-desktop-main.png`。
- [x] 2026-07-04 图片工作台稳定版收口已完成：右侧移除“历史作业”入口和对应组件，统一由“队列”承载新旧作业查看与跳转；`imageWorkbenchReview.ts` 新增统一资产关系上下文，当前审图、右侧详情、对比条和卡片徽标共用同一套来源 / 分支 / 交付关系计算，减少组件内重复推断。浏览器 mock 已验证生成 4 张、队列完成、选图审片和右侧详情正常，截图保存到 `output/playwright/image-workbench-stable-no-history.png`。

## 真实环境验证

- [x] 在真实 Tauri 窗口用 localhost OpenAI-compatible mock Provider（无 API Key）smoke `generate_ai_content` 的 `chat/image/audio/video`：通过 WebView2 CDP 触发前端 Store 诊断路径，确认文本返回、图片保存、音频 `/audio/speech`、视频 `/videos` 创建/轮询/同源下载、artifact 落盘、产物路径转换和本地队列成功/失败状态收敛。当前独立能力测试页面已移除，后续原子能力复核以 Provider 配置诊断或服务级测试为准。
- [x] 2026-06-14 已用 Windows Computer Use 复核真实 `Monster Tools` 桌面窗口：重启 debug 可执行文件后应用可加载，键盘导航可进入 `AI 模型工作台`，真实模型配置列表和 `Codex Local Gateway` / `localhost:4444/v1` 配置可见，API Key 在 UI 中为掩码显示。当前 Computer Use 坐标点击、移动和滚动接口对该 WebView 绑定不稳定，真实窗口复杂点击流仍不作为后端/Provider/DB 闭环阻塞项。
- [ ] 用有效真实音视频 Provider 复核 `generate_ai_content` 的媒体播放、真实视频文件有效性、API Key 脱敏日志/错误提示和长任务队列状态；localhost mock 的 video 仅验证二进制保存与 MIME/下载链路，不代表真实播放已闭环。
- [x] 2026-06-14 已在 browser mock 运行态复核 `/image-workbench`：文生图从页面生成收敛到 4/4，资产/元数据/model_run/历史分别为 4/4/4/1。普通浏览器中历史生图 `asset.localhost` 会报连接失败，这是非 Tauri 环境无法解析桌面 asset 协议导致；真实 WebView 仍需最终观感复核。
- [ ] Windows Computer Use 当前 `list_apps` 连续超时且状态诊断存在 bridge 空输入错误；直接 `get_app_state("Monster Tools")` 可截图窗口，但窗口复杂点击流绑定不稳定。后续真实窗口复杂 UI 复核仍优先用 WebView2 CDP / Playwright / 稳定截图方案，Computer Use 只作为可用时的辅助观感工具。
- [x] 2026-06-19 视频生成、高清放大等长耗时任务的 worker heartbeat 续租已闭环（`services/runtime_heartbeat.rs`，每 15s CAS 续租）；GLOBAL_WORKER_LIMIT 进程级 permit 已落地（`services/runtime_dispatcher.rs`），同时 running worker 数 ≤ 4。运行时准入规范已沉淀到 `docs/architecture-current-state.md` §6；前端仍只订阅或轮询轻量状态与 artifact metadata，直连原子测试不持久化完整 Provider config。
- [ ] 用真实第三方 Anthropic Messages 格式 API（`custom + anthropic-messages` adapter）和 OpenAI-compatible 自定义 Provider smoke AI 工作台 capability 展示、按钮禁用、聊天/生图可用性和错误提示；避免使用生产 API key 进入日志或文档。
- [x] `/image-workbench` 真实 Provider 成功链路已闭环：2026-06-13 使用 active image config `ai-model-1780916920189-5krffy` / `gpt-image-2` 生成 PNG，创建真实 job `iw-job-1781361589184-1`，写入 succeeded task、asset、metadata 和 succeeded model_run；资产文件存在于 `~/.monster-tools/ai/image-workbench/assets/iw-task-1781361589185-2/`，且大小与 DB `size_bytes` 一致。Windows UIA 截图自动化仍不稳定，后续只保留真实窗口最终观感/交互复核，不再阻塞后端/Provider/DB 闭环结论。
- [x] 2026-07-02 `/image-workbench` 真实窗口观感已完成本轮复核：真实 `Monster Tools` Tauri 窗口截图可见图片工作台、右侧详情、资产库、缩略图和导出区域；复杂点击流仍以 Playwright 浏览器 smoke 补足，避免受 Windows Computer Use / WebView 坐标绑定不稳定影响。
- [x] 2026-07-02 `/image-workbench` 新版“审片优先”首屏已完成本轮浏览器与真实窗口复核：浏览器 smoke 覆盖主流程和 390px 窄宽度布局，真实 Tauri 截图确认主界面可见；后续若继续拆资产组/版本链，以数据语义和组件边界为主，不再把首屏观感作为阻塞项。
- [x] 2026-07-03 `/image-workbench` 真实窗口图片显示已复核：重启 Tauri dev 窗口后，当前结果区、上方对比条和大图区域显示真实图片；最新全缺失导入作业仍会按 `missing` 状态显示占位，这是数据状态而不是渲染故障。
- [ ] `/image-workbench` 真实窗口系统文件对话框与复杂点击流仍需单独复核：包括目录选择、文件导出、拖拽/键盘 modal 操作等系统交互；优先用 WebView2 CDP、稳定截图或可控手动验收，Computer Use 只作为辅助。
- [ ] `/image-workbench` 在“审片优先”首屏之后，下一轮优先考虑资产组/版本链：让同一作业或同一风格延续结果有更清晰的归属关系，避免图库继续停留在缩略图平铺层。
- [ ] 图片工作台增强模式（图生图、局部重绘、人物一致性、高清放大）已进入 mode / capability / UI 降级清单；参考图/外部图入口已能自动切到图生图并显示近似反推提示词；局部重绘已补前端 mask 画布保存、PNG alpha mask 持久化、Sidecar mask PNG 校验和浏览器 mock inpaint job 闭环。2026-07-03 已补 `gpt-image-*` 原生编辑能力识别、`/images/edits` 官方 `image[]` multipart 字段、资产库多参考图上传和真实 `img2img/person_consistency/inpaint` smoke；真实编辑结果的多候选图入库与非请求尺寸展示策略已落地；后续还需补高清放大真实 Provider 验证。

## 文档维护

- [x] 2026-07-02 根目录旧文档入口已清理：删除重复且易漂移的 `CLAUDE.md`、旧规范索引 `DEV_STANDARDS.md` 和过时发布测试指南 `RELEASE_TEST_GUIDE.md`；README 已改为当前 Monster Tools / AI 图片工作台口径，发布说明收束为现行命令与 `AGENTS.md` 入口。
- [ ] 后续文档只更新当前事实、长期规则、边界和未闭环事项；不要重新创建历史 Goal 执行包、阶段提示词、平行路线图或过程性复核堆栈。
- [x] 2026-07-04 架构体量规则已从“行数硬失败”改为“完成后审查提醒”：`npm run check:architecture` 继续对分层、IPC、Tauri、DB、文件能力和 sidecar bundle 违规失败；文件行数超过参考线只输出审查提醒，不阻断验证。`AGENTS.md`、`docs/architecture.md`、`docs/engineering-playbook.md`、`docs/ai/review-checklist.md` 已同步新口径。
- [ ] 若新增、修改或删除规范文档，先读 `docs/ai/maintenance.md` 和 `docs/ai/index.md`；优先更新已有专题文档。

## 产品设计

- [x] 2026-07-03 已将 `/workspace` 从欢迎页收敛为工作台总览：移除大 hero 卡片，顶部改为紧凑总览条，继续展示 Provider 就绪、未完成任务、最近资产、高频入口和继续工作信息；同时新增通用 `workbench-page-shell` 页面壳，`/ai` 移除内部标题栏，`/navigation`、`/workspace`、`/file-manager`、`/settings`、`/system`、`/tools` 与 `/ai` 共享同一外层容器密度。Playwright 复核 1365x900 与 390x800，窄窗口下页面壳保留 360px 最小宽度以避免文字被挤成竖排。
- [x] 2026-07-04 `/image-workbench` 已从左侧表单页改为桌面工作台结构：左侧只保留新建任务和 6 个任务入口，中间画布承载场景选择、图库、审图和局部编辑，底部命令栏承载描述输入、常用尺寸/数量/参考图和更多设置，右侧继续作为“下一步”动作区。浏览器 mock 已用 1440x900 和 1920x1080 复核无横向溢出，场景预设可自动写入描述，参考图任务会在底部展开参考来源选择。
- [ ] 重新梳理主导航分组：按“创作 / 资产 / 工具 / 系统诊断”组织现有 AI、图片工作台、文件、导航、工具和设置模块。
- [ ] 为 `/image-workbench` 设计生产化工作流：补模式引导、资产组/版本链、模板配方复用、失败原因和下一步动作，避免只在现有三栏界面继续追加按钮。
- [x] 2026-07-06 功能重复已先收口：`/ai` 只保留模型配置、Provider 诊断和提示词库，旧独立对话、生图、能力测试页面及会话运行时不再作为当前产品事实；`/image-workbench` 承担批量生产、资产、版本链、任务和参考图编辑。文件管理器仍只做通用文件，图片工作台资产库只做带作业/版本/交付元数据的图片资产；图片工作台模板长期仍应和 AI 提示词库共享底层数据或明确分层，避免两套提示词资产并行增长。
- [ ] `/image-workbench` 已完成“审片优先 + 下一步分支卡片”的第三轮收束，下一步优先把“资产组 / 版本链”做成真实语义和界面承载，避免图库继续停留在平铺缩略图层。
- [ ] `/image-workbench` 已先基于 `job.sourceAssetId` 落地轻量“版本链”展示与跨 job 选图切快照；下一步继续补更完整的资产组 / 版本链数据语义，而不是只停留在 UI 提示层。
- [ ] `/image-workbench` 当前链路已能在图库分组和卡片徽标层表达“来源 / 已选 / 分支”，且“交付”已下沉为 `deliveryStatus`；下一步继续把来源/分支/链路层级和同源分支归属升级成更完整的资产组语义。
- [x] `/image-workbench` 已把“可交付结果”单独提升为 `library` 分组，并于 2026-07-02 下沉为 `asset.deliveryStatus === "ready"` 正式字段；后续若要做人工交付确认，可在该字段上扩展状态，而不是恢复前端推断。
- [ ] `/image-workbench` 的图库分组头已经能显示来源/分支/交付数量摘要；交付计数已读正式字段，来源/分支仍适合继续下沉为正式字段或资产组关系，而不是继续扩大前端推断范围。
- [x] 2026-07-03 图片工作台队列控制已补齐：右侧队列卡片支持取消队列，选中作业支持停止当前可运行任务；后端新增 `cancel_image_workbench_task`，单任务取消会写入 `cancelled` 状态、失败类型和 model_run 审计，并同步取消 generation task。队列状态重算已避免“只剩排队项却显示正在进行”，浏览器 mock 与真实后端保持一致；已验证 `npm run typecheck`、`npm run check:architecture`、`cargo test --manifest-path .\src-tauri\Cargo.toml image_workbench`、`cargo fmt --check --manifest-path .\src-tauri\Cargo.toml` 和 `git diff --check`。
- [x] 2026-07-03 图片工作台参考图与右侧下一步动作已校准：左侧移除“参考描述”推测区，只保留真实参考图；参考图限制为最多 4 张并在左侧显示计数，超过上限会提示。右侧“继续同一风格”会以当前选中图作为主参考，“局部修改”会清理旧参考和旧蒙版后进入涂抹流程，“延续人物设定”会围绕当前人物图设置参考和默认人物提示，“放大图片”只使用当前选中图并清理多参考干扰。已验证 `npm run typecheck`、`npm run check:architecture` 和 `git diff --check`。
